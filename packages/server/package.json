{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "check": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.12.23",
    "shared": "workspace:*",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/web-push": "^3.6.4",
    "typescript": "^6.0.2"
  }
}